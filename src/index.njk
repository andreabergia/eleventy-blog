---
layout: layouts/base.njk
title: Andrea Bergia
---

<h1>Welcome</h1>
<p>This is the emerging Eleventy version of Andrea Bergia's website.</p>

{% if collections.blog | length %}
<h2>Latest posts</h2>
<ul>
  {% for post in collections.blog | reverse %}
  <li>
    <a href="{{ post.url }}">{{ post.data.title }}</a>
    {% if post.date %}
      <span>— {{ post.date | readableDate }}</span>
    {% endif %}
  </li>
  {% endfor %}
</ul>
{% else %}
<p>No posts yet. Migration in progress.</p>
{% endif %}
