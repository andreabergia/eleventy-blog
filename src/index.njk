---
layout: layouts/page.njk
title: Andrea Bergia's Website
description: Andrea Bergia's Website – developer, gamer, cat lover, tea addict.
---

<div class="post-header-section">
  <div class="post-header-content">
    <div class="home-header">
      <eleventy-image src="{{ '/images/me2022-small-square.jpg' | base64 }}" alt="{{ site.author.name | base64 }}"></eleventy-image>
      <div class="home-header-text">
        <h1>Hi, I'm Andrea</h1>
        <p>Welcome! I'm an Italian 🇮🇹🇪🇺 software engineer, and this is my little space of the Internet.
        You can find here my blog, where I write about programming, compilers, Rust, Java, or about cool stuff I find on the web.
      </div>
    </div>
  </div>
</div>

<div class="post-content-section">
  <div class="post-content-wrapper">
    <div class="home-columns">
      {% if collections.featuredPosts and collections.featuredPosts.length %}
      <section>
        <h2 style="margin-top: 0;">Featured Posts</h2>
        <ul class="post-list post-list--stacked">
          {% for post in collections.featuredPosts %}
          <li>
            <a href="{{ post.url }}">
              <span class="post-meta">{{ post.date | readableDate }}</span>
              <span class="post-title">{{ post.data.title }}</span>
            </a>
          </li>
          {% endfor %}
        </ul>
      </section>
      {% endif %}

      {% set recentPosts = collections.recentPosts %}
      {% if recentPosts and recentPosts.length %}
      <section>
        <h2 style="margin-top: 0;">Recent Posts</h2>
        <ul class="post-list post-list--stacked">
          {% for post in recentPosts %}
          <li>
            <a href="{{ post.url }}">
              <span class="post-meta">{{ post.date | readableDate }}</span>
              <span class="post-title">{{ post.data.title }}</span>
            </a>
          </li>
          {% endfor %}
        </ul>
      </section>
      {% else %}
      <p>No posts yet. Migration in progress.</p>
      {% endif %}
    </div>

    <p style="margin-top: var(--space-xl); text-align: center;">
      <a href="/post/" style="font-weight: 600;">View all posts →</a>
    </p>
  </div>
</div>
