{% extends "layouts/base.njk" %}

{% block content %}
<article>
  <h1>{{ title }}</h1>
  {% if page.date %}
  <p>
    <time datetime="{{ page.date | dateIso }}">{{ page.date | readableDate }}</time>
  </p>
  {% endif %}
  {{ content | safe }}
</article>
{% endblock %}
