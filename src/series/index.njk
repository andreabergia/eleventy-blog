---
layout: layouts/page.njk
title: Series
permalink: /series/index.html
---

<div class="post-header-section">
  <div class="post-header-content">
    <h1>Series</h1>
  </div>
</div>

<div class="post-content-section">
  <div class="post-content-wrapper">
    {% if collections.series and collections.series.length %}
    <ul class="tag-grid">
      {% for series in collections.series %}
      <li class="tag-card">
        <a href="/series/{{ series.slug }}/">
          <span class="tag-name">{{ series.name }}</span>
          <span class="tag-count">{{ series.posts | length }} posts</span>
        </a>
      </li>
      {% endfor %}
    </ul>
    {% else %}
    <p>No series have been migrated yet.</p>
    {% endif %}
  </div>
</div>
